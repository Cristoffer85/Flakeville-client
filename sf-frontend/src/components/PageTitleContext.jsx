import React from 'react';

const PageTitleContext = React.createContext();   //Component from Reacts API context method to store page title in one context without having to repeat same and same code over again... uhh... nice.

export default PageTitleContext;